{
  "name": "mood-map-monorepo",
  "private": true,
  "scripts": {
    "dev:v1": "pnpm --dir app-v1 install && pnpm --dir app-v1 dev",
    "dev:v2": "pnpm --dir app-v2 install && pnpm --dir app-v2 dev",
    "build:v1": "pnpm --dir app-v1 install && pnpm --dir app-v1 build",
    "build:v2": "pnpm --dir app-v2 install && pnpm --dir app-v2 build",
    "build:all": "pnpm run build:v1 && pnpm run build:v2"
  },
  "dependencies": {
    "react-router-dom": "^7.8.0"
  },
  "devDependencies": {
    "@types/react-router-dom": "^5.3.3"
  }
}
